# Hello this is Suraj. We will look here program to add two number.

# Add two Numbers

# Defining the function named add which will add two numbers.
def add():
    p = a + b
    print(p)

# Taking two inputs from user and assigning them to a and b.
a = int(input("Enter first number: "))
b = int(input("Enter second number: "))

# Calling function add to add the two numbers
add() 
